#include <stdio.h>

/**
 * main : Entry point
 * puts : Write string in stdo
 * return : 0 (success)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
